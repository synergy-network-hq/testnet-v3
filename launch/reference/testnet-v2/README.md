# Retired Testnet-v2 reference — do not deploy

These files were moved out of active runtime paths while preparing
Testnet-v3. They exist only to compare allocations, topology, and operational
structure.

They contain the retired genesis, public validator identities, operational
manifest, and bootstrap configuration from Testnet-v2. They are rejected by
the Testnet-v3 launch gate and must not be copied into active paths.
