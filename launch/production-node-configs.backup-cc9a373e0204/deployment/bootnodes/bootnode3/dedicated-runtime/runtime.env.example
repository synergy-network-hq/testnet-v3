# Create the deployed runtime.env only after independently verifying the V3 binary.
# This example is not a deployable EnvironmentFile.
SYNERGY_RELEASE_RUNTIME_BINARY=
SYNERGY_RELEASE_RUNTIME_SHA256=
