# Validator Identity Fields

The complete allowlist of fields that may differ between validators is maintained in `template/IDENTITY_FIELDS.yaml`.

All other config, service, binary, genesis, chain-spec, consensus, DAG, mempool, logging, metrics, RPC, firewall, and control-panel values must be identical or generated from the same template.

