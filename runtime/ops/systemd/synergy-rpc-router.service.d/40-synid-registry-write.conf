[Service]
# The public router persists synergy_registerSynID records in this directory.
# ProtectSystem=strict otherwise mounts it read-only inside the service sandbox.
ReadWritePaths=/var/lib/synergy/rpc-gateway
