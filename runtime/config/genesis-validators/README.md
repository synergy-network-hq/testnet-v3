# Testnet-v3 genesis validator identities

Fresh public validator identity documents belong here after the offline
identity process. Never commit private or decrypted key material.

Do not reuse identities from the retired predecessor material under
`launch/reference/`.
