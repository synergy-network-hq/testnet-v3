# Synergy TESTNET Chain 1263 Stall - 2026-05-17

## Scope

This note documents the observed stall on the authorized Synergy public TESTNET chain 1263, the recovery action, and the remaining service-plane blocker.

Canonical chain identity:

- Network: `synergy-testnet`
- Chain ID: `1263`
- Hex chain ID: `0x4ef`
- Genesis hash: `dd9ad8cfc74be1ab17a0a0fce9db65281df1b325fe5a2530130dce8935e450b8`
- Network magic bytes: `d5d5bb99`

## Current Halt Snapshot

Captured at approximately `2026-05-17T13:19Z`.

| Node | Latest height | Latest hash | Status |
| --- | ---: | --- | --- |
| Public RPC / Node-RPC | 23364 | `1f95d5448ecfa9432efc1840354b0c439d1188ebd26cc8c94b3bd40eb35723af` | running, synced |
| Validator 1 | 23363 | `3033fa05ae135c1eaa747dc5f3765cc5ac7b5797f83150b1fffa93d2497d9419` | running, one block behind |
| Validator 2 | 23364 | `1f95d5448ecfa9432efc1840354b0c439d1188ebd26cc8c94b3bd40eb35723af` | running, synced |
| Validator 3 | 23364 | `1f95d5448ecfa9432efc1840354b0c439d1188ebd26cc8c94b3bd40eb35723af` | running, synced |
| Validator 4 | 23018 | `58c4a19753563c30c7d50d5e98c22802bd2d2571825895239374d209322b8917` | running, far behind |
| Validator 5 | 23364 | `1f95d5448ecfa9432efc1840354b0c439d1188ebd26cc8c94b3bd40eb35723af` | running, synced |

All observed validator peer records reported the canonical genesis hash. The stall is not explained by a genesis-hash fork.

## Immediate Stall Mechanism

The chain halted at height `23364` because height `23365` could not obtain the configured quorum of 4 validator votes.

Evidence:

- Validator 5 proposed height `23365` and logged:
  - `QC error - block proposal failed`
  - `Insufficient validator votes: 3 votes, 4 required for quorum`
- Validator 1 repeatedly refused vote requests for height `23365`:
  - `proposal height 23365 does not extend local tip 23363`
- Validator 4 repeatedly refused vote requests for height `23365`:
  - `proposal height 23365 does not extend local tip 23018`
- Validators 2, 3, and 5 were aligned at height `23364`, but that gives only 3 usable votes.

With Validator 4 far behind and Validator 1 missing height `23364`, the network had only three validators able to vote for `23365`. The consensus threshold remained 4, so every block proposal for `23365` failed.

## Catch-Up Failure

Validator 1 and Validator 4 both detected that peers were ahead and attempted catch-up, but the catch-up loop did not apply the missing blocks.

Validator 1 log pattern:

- local height: `23363`
- best validator height: `23364`
- error: `Timeout waiting for blocks up to height 23364`

Validator 4 log pattern:

- local height: `23018`
- best validator height: `23364`
- error: `Timeout waiting for blocks up to height 23082`

The relevant code path is:

- `src/consensus/consensus_algorithm.rs`
  - Before producing, validators call `get_best_validator_peer_height_with_support(...)`.
  - If the best validator height is ahead of local height, `sync_validator_to_network_tip(...)` calls `SYNC_MANAGER.start_sync()`.
- `src/sync/manager.rs`
  - Sync requests a reconciliation range starting at `local_height - 8`.
  - It waits for the target height to be applied.
  - If the target height is not reached before timeout, it returns `Timeout waiting for blocks up to height ...`.
- `src/p2p/networking.rs`
  - Incoming vote requests whose proposal does not extend the local tip trigger `request_vote_request_parent_sync(...)`.
  - The lagging validator waits only `900ms` for the parent to arrive before refusing the vote.

This means the runtime notices the lag, but the missing block batch is not applied quickly or reliably enough to make the validator eligible to vote before consensus moves on.

## Root-Cause Statement

The halt was caused by quorum loss after two validators became ineligible to vote for height `23365`:

1. Validator 4 fell behind to height `23018`.
2. Validator 1 failed to apply height `23364` and remained at height `23363`.
3. The remaining aligned validators, V2/V3/V5, could only provide 3 votes.
4. Chain 1263 requires 4 votes for quorum, so height `23365` could not finalize.
5. The built-in catch-up path repeatedly timed out and did not restore V1 or V4 to the network tip.

## Recovery Performed

Recovery began after the stall mechanism was reviewed. No genesis files were edited, no block hashes were regenerated, and no non-canonical chain was accepted.

Actions performed:

- Removed the direct public validator sessions from V1, V2, V3, and V5 by killing only the stale TCP sockets to `74.208.227.23`.
- Restored Node-RPC and Node-EXP to relayer-only peer configuration:
  - `snr://sentry1@relay1.synergy-network.io:5622`
  - `snr://sentry2@relay2.synergy-network.io:5622`
- Stopped all five genesis validator runtimes.
- Took the stopped V2 chain file as the canonical local recovery source after verifying:
  - SHA-256: `ddf05e6c492f65cf19831e62ff985dd3b6db9fb18d991ac9ff34cd2f1e83604e`
  - genesis hash: `dd9ad8cfc74be1ab17a0a0fce9db65281df1b325fe5a2530130dce8935e450b8`
  - tip: height `23364`, hash `1f95d5448ecfa9432efc1840354b0c439d1188ebd26cc8c94b3bd40eb35723af`
  - duplicate heights: `0`
  - parent-link breaks: `0`
- Backed up each validator's prior runtime files under `data/recovery-backups/20260517T141013Z/`.
- Replaced each validator's `data/chain.json`, `data/token_state.json`, and `data/validator_registry.json` with the verified canonical V2 files.
- Cleared transient consensus state on all five validators:
  - `data/consensus_vote_locks.json`
  - cached `data/consensus_proposals/*.json`
- Restarted all five validators.

Post-recovery validation:

| Check | Result |
| --- | --- |
| Validator public service ports | Closed from outside on V1-V5 for `5622`, `5640`, `5660`, `5680`, and `6030` |
| V1-V5 chain ID | `0x4ef` |
| V1-V5 genesis hash | `dd9ad8cfc74be1ab17a0a0fce9db65281df1b325fe5a2530130dce8935e450b8` |
| V1-V5 peer count | `4` on each validator |
| First advancing sample | V1-V4 at height `23441`, hash `fc10ff9adfd351d29b715176606599df883bbf671625b57bbcf60d8432461614`; V5 then reported height `23442` whose parent was that same `23441` hash |
| Later advancing sample | V1 reached height `23450`, proving block production resumed |

The live validator mesh is therefore back on one canonical chain and producing blocks.

## Additional Runtime Cause Found

The on-disk validator data showed that V1 and V4 had appended duplicate same-height local fork blocks:

- V1 had canonical height `23363` hash `9548a05e6a16812cf5db8f4a2354377a73741b90f0ee7208014e37f2c92c61bb`, followed by another height `23363` hash `3033fa05ae135c1eaa747dc5f3765cc5ac7b5797f83150b1fffa93d2497d9419`.
- V4 had canonical height `23018` hash `4629582b771a1642f41b378b44ad2a6e142db932b6668c44505eba39611e9f74`, followed by another height `23018` hash `58c4a19753563c30c7d50d5e98c22802bd2d2571825895239374d209322b8917`.

This was not a genesis fork. It was a local same-height append race.

Source-level fix staged in the workspace:

- `src/block.rs` now has `BlockChain::add_block_extending_tip(...)`, which rejects a same-height fork or wrong-parent append and treats an exact duplicate tip as already applied.
- `src/consensus/consensus_algorithm.rs` now uses that guard after the vote wait, before applying a committed local proposal to the in-memory chain.

This addresses the race where consensus releases the chain lock while waiting for votes, P2P advances the local tip, and the proposer later tries to append a stale block that no longer extends the current tip.

## Topology Misconfiguration Found After Review

The intended TESTNET topology is:

- Genesis validators and observer live on the WireGuard private plane.
- Public nodes, including RPC gateway, explorer/indexer, bootnodes, and seed servers, are public-facing.
- Public nodes should communicate with validator/observer infrastructure through the two relayers.
- Relayers should expose a public-facing surface to public nodes and a VPN-facing surface toward validators/observer.

Live inspection found violations of that topology:

- Node-RPC and Node-EXP had been manually rewired to dial validators directly as persistent peers. That was an operator error during the rollout and bypassed the relayer tier.
- Validators accepted direct public connections from `74.208.227.23` (`genesisrpc` and `genesisindexer`) in their peer tables.
- Validator processes bind P2P, RPC, WS, and metrics to `0.0.0.0`; V1-V3 public IPs were reachable from outside on `5622`, `5640`, `5660`, and `6030`.
- The validator runtime TOML advertises `10.69.0.x` private P2P addresses, but the host firewall/listen surface still exposes validator services publicly.
- Relayer WireGuard handshakes observed from validators were stale by roughly two days, and validator probes to relayer private service ports such as `10.69.0.20:5622`, `10.69.0.20:5640`, `10.69.0.202:5622`, and `10.69.0.202:5640` were closed.

This means the validator setup was not enforcing the intended private-only validator surface, and the relayer tier was not available as the required bridge. The direct RPC/indexer peering was not the correct fix for relayer unavailability; it should be removed and replaced with a working relayer path.

This topology issue does not change the immediate consensus symptom: height `23365` failed because only three validators could vote. It does explain why public service nodes could attach directly to validators and why the deployment drifted away from the intended relay-mediated design.

Follow-up status after recovery:

- Node-RPC and Node-EXP are canonical and relayer-only, but currently remain at height `23364` with `peer_count=0` because both relayer public P2P ports are closed.
- `relay1.synergy-network.io:5622`, `relay2.synergy-network.io:5622`, `195.26.241.95:5622`, and `94.72.117.108:5622` were closed from `synergyvps`.
- SSH to `195.26.241.95` and `94.72.117.108` was unreachable or closed from both this Mac and `synergyvps`, so relayer repair still requires a working access path or console session.
- Bootnode1, Bootnode2, and Bootnode3 are on chain `1263` and their `node.toml` files have no direct validator persistent peers.

## Important Non-Causes

- No observed active validator peer showed a non-canonical genesis hash.
- Public RPC was routed to the local chain 1263 Node-RPC and reported `synergy-testnet`.
- Disk and memory checks on the five validators did not show resource exhaustion.
- Validator processes were still running and connected; this was not a simple process crash.

## Remaining Follow-Up

Review and harden the catch-up behavior in `src/sync/manager.rs` and `src/p2p/networking.rs`, especially:

- why V1 did not receive/apply height `23364` from peers that had it;
- why V4 repeatedly timed out while requesting ranges from `23019` onward;
- whether vote-request parent sync should wait longer or synchronously request the exact missing parent from a peer known to have it;
- whether validators should refuse block production while any active genesis validator is known to be behind enough to break quorum.

Operationally, the next blocker is relayer access. Public RPC and Node-EXP are intentionally relayer-only now, but they cannot catch up until Relayer-1 and/or Relayer-2 are reachable and serving chain 1263 on their public and VPN surfaces.

## 2026-05-17 15:23Z Runtime Status

Validator liveness was rechecked after recovery:

| Validator | Latest observed state |
| --- | --- |
| V1 | Canonical genesis, `peer_count=4`, `sync_status=synced`, `block_time_secs = 2`, last 120 timestamp deltas all `2s` |
| V2 | Canonical genesis, `peer_count=4`, `sync_status=synced`, `block_time_secs = 2`, last 120 timestamp deltas all `2s` |
| V3 | Canonical genesis, `peer_count=4`, `sync_status=synced`, `block_time_secs = 2`, last 120 timestamp deltas all `2s` |
| V4 | Canonical genesis, `peer_count=4`, `sync_status=synced`, `block_time_secs = 2`, last 120 timestamp deltas all `2s` |
| V5 | Canonical genesis, `peer_count=4`, `sync_status=synced`, `block_time_secs = 2`, last 120 timestamp deltas all `2s` |

Relayer and observer reachability remained failed:

- Public TCP checks from the operator machine and from `synergyvps` could not reach Relayer-1 or Relayer-2 on SSH or P2P.
- Traceroute from the operator machine ended at the upstream hosting network with `Destination Host Unreachable` for both relayer public IPs.
- Validator-side VPN probes from V1 to Relayer-1 and Relayer-2 private addresses were closed for SSH, P2P, RPC, and metrics.
- V1 WireGuard state still showed stale relayer handshakes older than two days.
- Observer public SSH and observer VPN service probes were also unreachable/closed during this check.

Public service-plane status:

- Node-RPC and Node-EXP are still configured relayer-only, with no direct validator peers.
- Node-RPC and Node-EXP remain canonical but pinned at height `23364`, `peer_count=0`.
- Atlas API is running for chain `1263`, but its latest displayed block is also `23364` because its source RPC and fallback RPC are both pinned behind the unavailable relayer path.
- Two stale orphaned Atlas indexer processes were stopped on `synergyvps`; the systemd-managed Atlas API/indexer processes remain active.

Conclusion: the validator chain is live and holding a 2s cadence. The current public RPC/Atlas outage is a service-plane relay outage, not a validator consensus stall.

## 2026-05-17 16:45Z Relayer, Observer, RPC, and Atlas Update

Relayer repair was completed for both live relayer hosts:

- Relayer-1 and Relayer-2 now run `synergy-testnet-relayer.service`.
- Both relayers use the canonical chain `1263` genesis hash `dd9ad8cfc74be1ab17a0a0fce9db65281df1b325fe5a2530130dce8935e450b8`.
- Both relayers report `network = synergy-testnet`, `sync_status = synced`, `avg_block_time = 2.0`, and `peer_count = 7`.
- Both relayers listen on their VPN-facing service ports and on public P2P/RPC ports needed by the public service tier.

Public service topology was corrected:

- Node-RPC and Node-EXP on `synergyvps` are relayer-only again.
- Their persistent peers are only:
  - `snr://sentry1@relay1.synergy-network.io:5622`
  - `snr://sentry2@relay2.synergy-network.io:5622`
- No direct validator IPs remain in the Node-RPC or Node-EXP persistent peer configuration.

Public RPC and Atlas recovered through the relayers:

- Public RPC `https://testnet-core-rpc.synergy-network.io` reported chain `synergy-testnet`, chain ID `1263`, `avg_block_time = 2.0`, `peer_count = 2`, and latest block `27089`.
- Atlas API `https://testnet-atlas-api.synergy-network.io/api/v1/network/summary` reported `chainId = 1263`, `latestBlock = 27087`, `avgBlockTimeSeconds = 2`, `activeValidators = 5`, `totalValidators = 5`, and `peerCount = 2`.
- The Atlas lag at that sample was two blocks, which is normal while indexing a moving 2s chain.

Observer and observability status:

- The observer host is active on the WireGuard/private observability plane.
- `synergy-testnet-observer`, Prometheus, Grafana, and node exporter are all active.
- Prometheus target health reports `29` active targets up and no down targets.
- The observer Synergy process is back on the canonical live binary hash `b92bcee7e0c3a7d8957d7a2a710489ca55215900fcfdf4c7af3e2875969649ff`.
- The observer Synergy metrics listener was tightened to `127.0.0.1:6030` for local Prometheus scraping.

Important observer source defect found:

- The `observer_light_node` runtime worker expects P2P to be available so it can discover network height and sync observer status.
- The role profile did not include `p2p` or `chain-sync`, so the current released binary disables P2P and chain sync for observer-light.
- This leaves the observer process itself as metrics-only and causes `data/observer-light.json` to report `p2p network unavailable`.
- A source fix is staged in `src/role_profiles.rs` to add `p2p` and `chain-sync` to the observer-light service surface and to add a regression test.
- A Linux build from the older `synergyvps` checkout was rejected for live deployment because it failed the canonical genesis state-root validation. The previous canonical observer binary was restored. No live genesis data or validator chain data was altered.

Renewed same-height fork incident and recovery:

- After relayer repair, a renewed same-height split was observed at height `26369`.
- Majority branch: V1, V3, V4, and both relayers had hash `b9071642cc6b7fb72723b49e6cf18f26ba9a7c4e39e7887ca9e18b97e9abf0c7`.
- Minority branch: V2 and V5 had hash `2d0913cab0df6897c65870e2907dd4aefa25349faf11b4dd989f706f005b8bad`.
- Both branches shared parent `f18d417add99e591a861b45f28e244b5dfff287733fa277268308e9e552afc5e`.
- This confirmed the same-height append race had recurred because the staged source fix had not yet been deployed to validator binaries.

Recovery was performed without changing genesis:

- All five validators were stopped before canonicalization.
- A canonical recovery chain was reconstructed through height `26369` on the majority `b907...` branch.
- Recovery bundle SHA-256 for `chain.json`: `83b749137ab2dd5812728f4093fea514f46e85caa0805a732028e3d287353bca`.
- Recovered tip: height `26369`, hash `b9071642cc6b7fb72723b49e6cf18f26ba9a7c4e39e7887ca9e18b97e9abf0c7`.
- Genesis remained `dd9ad8cfc74be1ab17a0a0fce9db65281df1b325fe5a2530130dce8935e450b8`.
- The recovery chain had `0` duplicate heights and `0` parent-link breaks.
- `chain.json`, `token_state.json`, and `validator_registry.json` were applied to V1-V5, transient vote locks and cached proposals were cleared, and the validators were restarted.

Post-recovery validator and relayer status:

- V1-V5 and both relayers are advancing again on one branch.
- A post-recovery sample showed V1-V4 at height `27088`, hash `9f7c89832ec56fcf5932174e1fa172758c1b83cadf67763f6b58013e9541b886`, while V5 and both relayers had advanced one block to height `27089`, hash `8e966f76ca95cb38aa1d872ed0c9541f6c7ab4a4836f2566bbf48d6e533f0946`, whose parent was the height `27088` hash.
- This is normal sampling skew on a live 2s chain, not a fork.

Current conclusion:

- The relayer tier is repaired and is the active bridge for public RPC and Atlas.
- Atlas and public RPC are displaying live chain `1263` data with 2s block time.
- Prometheus and Grafana are active with all targets up.
- The observer process is operational as a metrics-only released observer, but the observer-light P2P/chain-sync source defect still needs to be released from the canonical source/release pipeline.
- The durable validator-side same-height fork fix is staged in source but still must be released and rolled out through the Node Control Panel/update path before new validator onboarding resumes.
