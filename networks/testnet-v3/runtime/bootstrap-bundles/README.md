# Testnet-v3 bootstrap bundles

This directory is intentionally empty until fresh Testnet-v3 identities,
signed release binaries, and the signed Testnet-v3 genesis are available.

Generate bundles from canonical tooling. Do not copy bundles from the retired
predecessor material under `launch/reference/`.
