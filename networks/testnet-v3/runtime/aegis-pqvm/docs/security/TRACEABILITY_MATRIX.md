# Security Traceability Matrix - Aegis PQVM

| Requirement ID | Security Requirement | Control Implementation | Verification Evidence |
|---|---|---|---|
| RQ-VM-001 | Deterministic/on-chain interfaces must never accept secret-key-bearing payload operations. | `src/integrations/abi.rs` deterministic encode/dispatch checks; deterministic adapter modules under `src/integrations/`. | `tests/integrations_dispatch.rs`, `tests/side_channel_review.rs`, `scripts/run_side_channel_review.sh` |
| RQ-VM-002 | ABI decoding and dispatch must fail closed for malformed/adversarial payloads. | Bounded decode/encode in `src/integrations/abi.rs`; strict routing checks in adapter modules. | `tests/vm_validation.rs`, `tests/fault_injection_campaign.rs`, `scripts/run_fuzz_fault_campaign.sh` |
| RQ-VM-003 | Beacon proof commitments must be cryptographically bound to proof-critical fields and verified. | Commitment construction/verification in `src/quantum_randomness_beacon.rs`. | `tests/quantum_randomness_beacon.rs` |
| RQ-VM-004 | Beacon policy controls (entropy source count, hardware requirement, epoch cadence) must be enforced. | Policy enforcement and verification logic in `src/quantum_randomness_beacon.rs`. | `tests/quantum_randomness_beacon.rs` |
| RQ-VM-005 | Entropy and randomness code paths must report failures without panic-based host DoS. | Error-return handling in `src/quantum_randomness_beacon.rs` and `src/utils.rs`. | `tests/quantum_randomness_beacon.rs`, `cargo test --all-features --locked` |
| RQ-VM-006 | Integration adapters must preserve declared routing semantics and contract binding. | Move route matching (`src/integrations/move/mod.rs`) and CosmWasm envelope binding (`src/integrations/cosmwasm/mod.rs`). | `tests/vm_validation.rs`, `scripts/run_side_channel_review.sh` |
| RQ-VM-007 | Fuzzing and fault-injection campaigns must execute with deterministic configuration and crash-free outcomes. | `fuzz/fuzz_targets/*`, `tests/fault_injection_campaign.rs`, `scripts/run_fuzz_fault_campaign.sh`. | `artifacts/security/fuzz_fault_campaign_*.json`, `artifacts/security/fuzz_campaign_*.log`, `artifacts/security/fault_injection_*.log` |
| RQ-VM-008 | Cryptographic source inputs used at build time must be pinned and checked for non-production markers. | Source-root pinning in `build.rs`; marker policy in `scripts/check_effective_pqc_source_markers.sh`. | `scripts/run_quality_gates.sh` marker step output |
| RQ-VM-009 | Release artifacts must support integrity/provenance validation. | `scripts/generate_release_manifest.sh`, `scripts/generate_sbom.sh`, `scripts/package_customer_bundle.sh`. | `artifacts/checksums/release-manifest.txt`, `artifacts/checksums/release-manifest.sha256`, `artifacts/sbom/aegis-pqvm.cdx.json`, `artifacts/package/*.tgz.sha256` |
| RQ-VM-010 | Shipment requires security review sign-off with zero unresolved high/critical findings. | Structured sign-off record in `docs/security/INDEPENDENT_SECURITY_SIGNOFF.md`; gate check in `scripts/check_independent_security_signoff.sh`. | `scripts/check_independent_security_signoff.sh`, `docs/security/INDEPENDENT_SECURITY_SIGNOFF.md` |
