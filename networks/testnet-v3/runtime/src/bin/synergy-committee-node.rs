fn main() {
    eprintln!(
        "synergy-committee-node is retired: committee is not a node role. Use an authorized Validator Node for any governed validator duty."
    );
    std::process::exit(2);
}
