# Validator crashes are handled by the Chain 1266 incident controller. A
# blanket systemd loop would destroy first-failure evidence and can cycle the
# entire quorum, so validators never restart automatically.
[Unit]
StartLimitIntervalSec=0

[Service]
Restart=no
